name: DCO Check

on:
  pull_request:
    branches:
      - '**'

jobs:
  dco:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check DCO
        uses: tisonkun/actions-dco@v1